# Required for live blueprint generation.
# Without it the app runs in DEMO MODE and serves a bundled sample blueprint,
# so you can click through the whole product with zero setup and zero spend.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# WONDERWHICH_MODEL=claude-opus-5
# WONDERWHICH_EFFORT=high
